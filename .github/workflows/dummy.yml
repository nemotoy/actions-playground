name: dummy
on:
  push:
    branches:
      - dummy
    paths:
      - 'dummy**'
jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello, dummy!"
  job3:
    runs-on: ubuntu-latest
    needs: job1
    steps:
      - name: check
        if: github.ref != 'refs/heads/master'
      - run: echo "Hello, job3!"
